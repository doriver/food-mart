<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.food_mart.modules.order.domain.mapper.OrderItemMapper">

    <select id="sumWeeklyCountByItemId" resultType="WeeklySalesRow">
        SELECT oi.item_id AS itemId, SUM(oi.count) AS cnt
        FROM order_item oi
        JOIN `order` o ON o.id = oi.order_id
        WHERE oi.created_at >= #{since}
          AND o.status IN
          <foreach item="s" collection="statuses" open="(" separator="," close=")">
              #{s}
          </foreach>
        GROUP BY oi.item_id
    </select>


    <select id="sumDailyCountByItemId" resultType="DailySalesRow">
        SELECT oi.item_id AS itemId, oi.name AS itemName, SUM(oi.count) AS salesCount
        FROM order_item oi
        JOIN `order` o ON o.id = oi.order_id
        WHERE oi.created_at >= #{from}
          AND oi.created_at &lt; #{to}
          AND o.status IN ('DELIVERY', 'COMPLETE')
        GROUP BY oi.item_id
    </select>

</mapper>

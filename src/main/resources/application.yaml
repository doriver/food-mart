spring:
  application:
    name: food-mart
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db-415fh8.vpc-cdb.ntruss.com:3306/mart?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: leesang
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: 'false'
        use_sql_comments: 'false'
        auto_quote_keyword: 'true'
        show_sql: 'false'
        default_batch_fetch_size: 10
    database-platform: org.hibernate.dialect.MySQLDialect